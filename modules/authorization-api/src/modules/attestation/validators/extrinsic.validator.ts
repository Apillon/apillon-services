import { ModelBase } from '@apillon/lib';
import { SubmittableExtrinsic } from '@kiltprotocol/types';

/**
 * Validate extrinsicValidator
 * @returns
 */
// export function extrinsicValidator() {
//   return async function (this: ModelBase, value: string): Promise<boolean> {
//     return typeof value == typeof SubmittableExtrinsic ? true : false;
//   };
// }
