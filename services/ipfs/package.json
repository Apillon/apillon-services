{
  "name": "at-storage-service",
  "version": "1.0.0",
  "author": "Kalmia d.o.o.",
  "license": "UNLICENSED",
  "description": "",
  "private": true,
  "scripts": {
    "prebuild": "rimraf dist",
    "test-service": "jest --passWithNoTests",
    "run-test-server": "cross-env APP_ENV=test nodemon ./src/scripts/dev/run-server.ts --inspect --inspect-brk=60002",
    "build": "tsc",
    "dev": "nodemon ./src/scripts/dev/run-server.ts --inspect --inspect-brk=60002",
    "lint": "tsc && eslint \"{src,apps,libs,test}/**/*.ts\" --fix",
    "deploy:prod": "serverless deploy --stage production",
    "deploy:staging": "serverless deploy --stage staging",
    "deploy:dev": "serverless deploy --stage dev"
  },
  "dependencies": {
    "@crustio/crust-pin": "^1.0.0",
    "@crustio/type-definitions": "^1.3.0",
    "@middy/core": "^2.5.7",
    "@polkadot/api": "^9.5.1",
    "@polkadot/keyring": "^10.1.9",
    "@polkadot/util": "^10.1.10",
    "at-lib": "*",
    "ipfs-http-client": "^56.0.3"
  },
  "devDependencies": {
    "eslint-config-common": "*",
    "tsconfig": "*"
  }
}
