{
  "name": "monitoring",
  "version": "1.0.0",
  "author": "Kalmia d.o.o.",
  "license": "UNLICENSED",
  "description": "",
  "private": true,
  "devDependencies": {
    "tsconfig": "*",
    "eslint-config-common": "*"
  },
  "scripts": {
    "test": "jest",
    "prebuild": "rimraf dist",
    "build": "tsc",
    "lint": "tsc && eslint \"{src,apps,libs,test}/**/*.ts\" --fix"
  }
}