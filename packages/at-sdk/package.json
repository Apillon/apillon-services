{
  "name": "at-sdk",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index",
  "types": "index.d.ts",
  "author": "Kalmia d.o.o.",
  "license": "UNLICENSED",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc",
    "test": "jest",
    "lint": "tsc && eslint \"{src,apps,libs,test}/**/*.ts\" --fix"
  },
  "devDependencies": {
    "eslint-config-common": "*",
    "tsconfig": "*"
  },
  "dependencies": {
    "aws-sdk": "^2.1205.0",
    "dotenv": "^16.0.2"
  }
}